{
  "name": "tharka-marks-card",
  "version": "1.0.0",
  "private": true,
  "description": "Tharka High School — individual student marks card for Classes 6-9 and the T30 batch",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
